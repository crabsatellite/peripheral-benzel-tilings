import BenzelProblem6Kernel.GeometricBacktrackReduction

/-!
# An exposed cell side is on the literal placement boundary

Only the positive first-coordinate neighbor (`side₅`) is needed for the
deterministic rightmost-cell peeling rule.  The theorem is checked against all
four literal prototiles and every one of their three constituent cells.
-/

namespace BenzelProblem6Kernel

end BenzelProblem6Kernel
